class Solution:
    def plusOne(self, digits: List[int]) -> List[int]:
        n = len(digits)
        a = ''
        ls = []

        for digit in digits:
            a += str(digit)
        for d in a:    
          
            ls.append(c)
        return c